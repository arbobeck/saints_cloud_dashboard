[build]
  command = "npm ci && npm run build --configuration=production"
  publish = "dist/saints-ui/browser"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
